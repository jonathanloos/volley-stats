<%= bootstrap_form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.collection_select :home_team_id, Team.all, :id, :name, label: "Home Team" %>
  </div>

  <div>
    <%= form.collection_select :away_team_id, Team.all, :id, :name, label: "Away Team" %>
  </div>

  <div>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.datetime_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
